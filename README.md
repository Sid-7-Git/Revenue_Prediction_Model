# Revenue_Prediction_Model
This notebook predicts revenue using ML. It handles data preprocessing (scaling, one-hot encoding), splits data into train/test sets, and trains and evaluates multiple regression models (XGBoost, LightGBM, Linear Regression with Pipeline) It covers the full ML workflow from data prep to prediction.
